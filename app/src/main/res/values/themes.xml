<resources>
    <!-- Theme for MainActivity -->
    <style name="Theme.MechanicalKeyboardPro" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Theme applied to the IME window itself: no title bar, no dim,
         sized to wrap the keyboard content instead of filling the screen. -->
    <style name="Theme.MechanicalKeyboardPro.Ime" parent="android:Theme.Material.Light">
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.InputMethod</item>
    </style>
</resources>
